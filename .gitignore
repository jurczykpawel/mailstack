node_modules/
dist/
.dev.vars
.env
.wrangler/
.DS_Store
priv/

# Business config (gitignored — bootstrapped from *.example via `npm run setup`).
# wrangler.toml is NOT here: it's committed (holds no secrets, just KV ids + your domain).
src/brands.ts
src/assets/logo.ts
docs/superpowers/
